cmake_minimum_required(VERSION 3.10)
project(GrapheMarkov C)

set(CMAKE_C_STANDARD 11)

# paths to the real source files (relative to this CMakeLists file)
set(PROJ_DIR ${CMAKE_CURRENT_LIST_DIR}/..)

add_executable(part1
    ${PROJ_DIR}/main.c
    ${PROJ_DIR}/part1.c
)
